{% extends "::base.html.twig" %}
{% block body %}
    <div class="container">
        <div class="row">
            <h1>{{ page.title }}</h1>
            <div class="span12">
               {{ page.content|raw }}
            </div>
        </div>
    </div>
{% endblock %}